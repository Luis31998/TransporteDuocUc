// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyCFPY8pQQCfKmkRPOIs4Pa1iklju6tulIY",
    authDomain: "transporteduocuc.firebaseapp.com",
    projectId: "transporteduocuc",
    storageBucket: "transporteduocuc.appspot.com",
    messagingSenderId: "752245715508",
    appId: "1:752245715508:web:bdc9245e68350dd3b81061",
    measurementId: "G-JCCV2JX8H3"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
